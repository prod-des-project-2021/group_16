@page "/signout"

@code {

    [Inject]
    public IAuthenticationService AuthSerivce { get; set; }

    [Inject]
    public NavigationManager NavManager { get; set; }

    protected override async Task OnInitializedAsync()
    {
        await AuthSerivce.SignOut();
        NavManager.NavigateTo("/");
    }
}
